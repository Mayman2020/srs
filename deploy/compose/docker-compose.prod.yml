# Overlay: production Spring profile. TLS + HSTS should be enforced on Nginx/Ingress; Spring HSTS off when behind TLS termination.

services:
  backend:
    environment:
      SPRING_PROFILES_ACTIVE: prod
      AC_SECURITY_HSTS_ENABLED: "false"
